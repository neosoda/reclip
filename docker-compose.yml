version: '3.8'

services:
  reclip:
    build: .
    image: reclip:latest
    container_name: reclip
    ports:
      - "8899:3000"
    volumes:
      - reclip_data:/app/downloads
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped

volumes:
  reclip_data:
