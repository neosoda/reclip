version: '3.8'

services:
  reclip:
    build: .
    image: reclip:latest
    container_name: reclip
    ports:
      - "8899:8899"
    volumes:
      - reclip_data:/app/downloads
    environment:
      - PORT=8899
      - HOST=0.0.0.0
    restart: unless-stopped

volumes:
  reclip_data:
